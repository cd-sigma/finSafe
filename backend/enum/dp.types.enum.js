/*
  description: contains data types for the dp values
    usage : used to get particular data type for dp (used in dp controller in internal)
 */

const dpTypesEnum = Object.freeze({
    BOOLEAN: "BOOLEAN",
    STRING: "STRING",
    ARRAY: "ARRAY",
    NUMERIC: "NUMERIC",
})

module.exports = dpTypesEnum
